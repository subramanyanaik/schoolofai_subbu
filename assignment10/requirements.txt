# The harness itself.
torch>=2.4
numpy>=1.26
tiktoken>=0.7
matplotlib>=3.8

# Only for the repo tooling, not for running the notebook:
#   tools/build_notebook.py --execute   (rebuilds truth_harness.ipynb with outputs)
#   pytest tests/                       (the correctness tests)
nbformat>=5.10
nbclient>=0.10
ipykernel>=6.29
pytest>=8.0
