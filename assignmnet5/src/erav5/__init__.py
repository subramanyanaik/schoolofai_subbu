"""erav5 — ERA V5 mixture & curriculum plan, computed rather than asserted.

Every figure in the README is produced by this package. Entry points:

    python3 -m erav5.run_plan    # full plan, 11 sections, 49 assertions
    python3 -m erav5.export      # machine-readable JSON dump

Exit status is non-zero if any consistency assertion fails.
"""
