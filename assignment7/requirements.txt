# Core -- the test suite, E1, E2, E3 and the parameter audit need only these.
torch>=2.4
numpy>=1.26
tiktoken>=0.7
pytest>=8.0
matplotlib>=3.8

# Corpus paths only (prepare_data.py, build_ood_corpus.py).
datasets>=3.0
pyarrow>=15.0

# Optional: only behind a TLS-intercepting corporate proxy.  Makes
# huggingface_hub validate against the OS certificate store instead of
# certifi's bundle.  It does NOT disable verification.
# truststore
